import 'package:flutter/material.dart';

class ProfileEditingModal extends StatelessWidget {
  const ProfileEditingModal({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
